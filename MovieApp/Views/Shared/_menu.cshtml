@model ProductCategoryModel
<div class="list-group">
    @foreach (var item in Model.Categories)
    {
        <a href="#" class="list-group-item list-group-item-action active">@item.CategoryName</a>

    }
</div>